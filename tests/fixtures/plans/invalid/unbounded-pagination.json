{
  "hosts": ["api.example.com"],
  "steps": [{
    "id": "forever", "method": "GET", "url": "https://api.example.com/items", "headers": {},
    "pagination": "cursor",
    "expect": {"default": {"route": "error", "map": {"reason": "response.error"}}}
  }]
}
